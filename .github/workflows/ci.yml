name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  ci:
    uses: OnlineChefGroep/.github/.github/workflows/ci.yml@master
    with:
      language: node
